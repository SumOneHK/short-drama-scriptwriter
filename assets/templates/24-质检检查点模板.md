# {项目名} 质检检查点

## 全局问题追踪表
| ID | 日期 | 任务编号 | 阶段 | 复检方式 | 检查对象 | 等级 | 问题摘要 | 状态 | 必改时点 |
|----|------|---------|------|---------|---------|------|---------|------|---------|
| QC-001 | {日期} | T10/T11 | 大纲质检 | clean-context | 分集大纲.md | P1 | 中段重复桥段偏多 | 新发现 | 进入剧本前 |

状态说明：
- `新发现`
- `处理中`
- `已修复`
- `接受风险`
- `延后处理`

## {日期} /{质检命令}
- 检查对象：
- 检查范围：
- 任务编号：`Txx`
- 当前阶段：
- 复检方式：`clean-context / subagent（用户明确并行或代理授权）`
- clean-context 证据：
  - `lastIndependentReviewMode`：
  - `evidenceTimestamp`：
  - `reviewRunner`：`user-new-session / isolated-review-task / subagent`
  - `contextIsolation`：
  - `evidenceFiles`：
  - `evidenceInstruction`：
  - `scriptCheckResult`：
- 结论：`通过 / 需修改 / 阻塞`
- 是否允许进入下一步：`是 / 否`
- P0：
- P1：
- P2：
- 必改项：
- 可延后项：
- 状态回写：
  - `currentStep`：
  - `lastQcStep`：
  - `lastIndependentReviewMode`：
  - `qcStatus`：
  - `changeControl`：
- 下一步：
- 问题状态更新：
